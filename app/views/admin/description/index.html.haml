- content_for :sidebar do
  = render :partial => "sidebar/admin_description"

#description-admin
  #group
    .title
      %h1 Pages


  - if @type == :primary_category
    %h3= @primary_category
  - elsif @type == :sub_category
    %h3= @sub_category.category

  .pages-table
    = form_tag admin_description_index_path, :method => :post do
      %table#admin-table.table.table-striped
        %thead
          %tr
            %th.yellow{:width => "50px"} Date
            %th.green{:width => "150px"} Group
            %th.red Author
            %th.purple{:width => "50px"} Execs
            %th.purple{:width => "50px"} Desc.
            %th.purple{:width => "30px"} CI
            %th.purple{:width => "50px"} Events
            %th.blue Page
            %th.blue History
        = render :partial => "description", :collection => @descriptions
      
      .actions.form
        = submit_tag "Submit", :class => "btn primary"
